<view class="searchLetter touchClass">
  <view wx:for="{{searchLetter}}" style="height:{{itemH}}px" wx:key="index" data-letter="{{item.name}}" catchtouchstart="searchStart" catchtouchmove="searchMove" catchtouchend="searchEnd">{{item.name}}</view>
</view>

<block wx:if="{{isShowLetter}}">
  <view class="showSlectedLetter">
    {{showLetter}}
  </view>
</block>
<!-- <view class='top'>
  当前城市/位置
</view>
<view class='top_title'>
  <text>上海</text>
  <text class='top_text'><image class='top_map' src='/img/iconfig.png'></image> 上海 实惠路</text>
</view>
<view class='top'>
  热门城市
</view>
<view class='top_city'>
  <text>上海</text>
</view>  -->
<view>
  <view class='top'>
    {{dw}}
  </view>
  <view class='top_title'>
    <text>{{top_title}}</text>
    <view class='top_text' wx:for='{{top_text}}'> {{item.city }}  {{ item.region }}</view>
  </view>
  <view class='top'>
    {{hotcity}}
  </view>
  <view class='top_city'>
    <text wx:for='{{hot}}'>{{item.city}}</text>
  </view>
</view>
<scroll-view scroll-y="true" style="height:{{winHeight}}px" bindscroll="bindScroll" scroll-top="{{scrollTop}}">
  <view class="selection" wx:for="{{cityList}}" wx:key="{{item.initial}}">
    <view class="item_letter">{{item.initial}}</view>
    <view class="item_city" wx:for="{{item.cityInfo}}" wx:for-item="ct" wx:key="{{ct.id}}" data-city="{{ct.city}}" bindtap="bindCity">
      {{ct.city}}
    </view>
  </view>
</scroll-view>